<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_new_journal"
        android:orderInCategory="100"
        android:title="@string/new_journal"
        android:icon="@drawable/ic_note_add_24dp"
        app:showAsAction="ifRoom"/>

    <item android:id="@+id/action_clear_data"
        android:orderInCategory="200"
        android:title="@string/clear_all_journals"
        app:showAsAction="never"/>

    <item android:id="@+id/action_resources"
          android:title="@string/resources"
          android:orderInCategory="300"
          app:showAsAction="never"/>

    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="400"
        app:showAsAction="never"/>

</menu>